# ######################## tb101_cp_server配置 ###########################
# 默认配置文件位置为 当前工作目录下的 config/tb101_cp_server.toml

# ####################### 备份配置 ###########################

[BACKUP]

# 若在Docker容器内运行，则为Docker容器内部路径, 如果为相对路径则相对于配置文件(settings.toml)所在目录的路径

# 备份主目录
path = "../workspace/backup/"
